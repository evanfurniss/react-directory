import React from "react";
import SearchForm from "../SearchForm";

function Search(){
    
    return(
        <wrapper>
            <div>
                <SearchForm />
            </div>
        </wrapper>
    );
};

export default Search;